{
  "name": "eleventy",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun cleanup && bunx @11ty/eleventy",
    "start": "bunx @11ty/eleventy --serve",
    "cleanup": "rm -fr dist",
    "dev": "bun cleanup && bunx @11ty/eleventy"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "react-dom": "^19.1.1",
    "server": "react-dom/server",
    "tsx": "^4.20.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
